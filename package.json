{
  "name": "new-order-avalanche",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ brokenomics/new-order-avalanche",
    "create-local": "graph create --node http://localhost:8020/ brokenomics/new-order-avalanche",
    "remove-local": "graph remove --node http://localhost:8020/ brokenomics/new-order-avalanche",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 brokenomics/new-order-avalanche"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
