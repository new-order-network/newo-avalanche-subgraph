{
  "name": "new-order-avalanche",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ new-order-network/new-order-avalanche",
    "create-local": "graph create --node http://localhost:8020/ new-order-avalanche",
    "remove-local": "graph remove --node http://localhost:8020/ new-order-avalanche",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 new-order-avalanche"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
